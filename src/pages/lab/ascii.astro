---
import { i18n } from "../../lib/i18n";
const { t } = i18n(Astro.url);
import Base from "../../layouts/Base.astro";
import { site } from "../../data/site";
---
<Base title={`ASCII lab · ${site.name}`} description={t("Algorithms, mechanisms, and shapes in dots and characters.", "Algoritmi, meccanismi e forme in punti e caratteri.")} lab />
